import sys
import gdb

# Update module path.
dir_ = '@datadir@/valadate/gdb'
if not dir_ in sys.path:
    sys.path.insert(0, dir_)

from valadate_gdb import register
register (gdb.current_objfile ())
